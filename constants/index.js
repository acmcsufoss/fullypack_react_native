//This file simply export all files in the current folder so we can use it anywhere

import { COLORS, FONT, SIZES } from "./theme";
import images from "./images";
export { COLORS, FONT, SIZES, images };
